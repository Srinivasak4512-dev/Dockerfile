
#The ADD instruction in a Dockerfile is used to copy files or directories from the host system into the container. 
#It is similar to COPY, but with additional capabilities:

#✅ It can extract .tar archives automatically.
#✅ It can fetch files from remote URLs (though not recommended).

